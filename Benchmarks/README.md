# Orleans MongoDB Benchmarks

Executes various synthetic benches to determine implementation quality.

```shell
# test the default Reminder storage with a load factor
dotnet run --configuration Release -- -c 10
```